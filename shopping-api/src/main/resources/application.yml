spring:
  data:
    mongodb:
      uri: mongoDB{URI}
      database: shopping-api
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG